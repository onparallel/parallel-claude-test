/* eslint-disable no-restricted-imports */

// Docs: https://chakra-ui.com/docs/components/flex

// Flex changes spacing to gap in v3
// In v3 gap is directly supported, for now we re-export as-is
export { Flex } from "@chakra-ui/react";
export type { FlexProps } from "@chakra-ui/react";
