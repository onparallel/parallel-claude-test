{
  "private": true,
  "workspaces": [
    "bin",
    "client",
    "emails",
    "server",
    "e2e"
  ],
  "scripts": {
    "lint": "eslint --config .eslintrc.js .",
    "update-deps": "ncu -u && ncu -u --packageFile bin/package.json && ncu -u --packageFile client/package.json && ncu -u --packageFile server/package.json  && ncu -u --packageFile e2e/package.json",
    "postinstall": "patch-package",
    "postpull": "yarn && cd server && yarn migrate && cd .. && yarn generate-types",
    "generate-types": "cd server && yarn generate-graphql-types && yarn generate-db-types && cd ../client && yarn generate-graphql-types"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.28.3",
    "@typescript-eslint/parser": "^4.28.3",
    "eslint": "^7.30.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-formatjs": "^2.17.1",
    "eslint-plugin-graphql": "^4.0.0",
    "eslint-plugin-prettier": "^3.4.0",
    "eslint-plugin-react": "^7.24.0",
    "npm-check-updates": "^11.8.3",
    "postinstall-postinstall": "^2.1.0",
    "prettier": "^2.3.2"
  },
  "dependencies": {
    "patch-package": "^6.4.7"
  },
  "_resolutions_why": {
    "@apollo/client": "@types/apollo-upload-client is depending on an old version which is causing build issues"
  },
  "resolutions": {
    "@apollo/client": "^3.3.21"
  }
}
