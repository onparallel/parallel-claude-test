{
  "private": true,
  "workspaces": [
    "bin",
    "client",
    "emails",
    "server"
  ],
  "scripts": {
    "update-deps": "ncu -u --packageFile bin/package.json && ncu -u --packageFile client/package.json && ncu -u --packageFile emails/package.json && ncu -u --packageFile server/package.json"
  },
  "devDependencies": {
    "npm-check-updates": "^4.0.1"
  }
}
