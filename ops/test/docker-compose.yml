version: "3"
services:
  db:
    image: postgres:12
    environment:
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=test
      - POSTGRES_DB=parallel_test
    ports:
      - "5433:5432"
    volumes:
      - /var/lib/postgresql/data
